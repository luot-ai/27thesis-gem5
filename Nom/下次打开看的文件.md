# 下次打开先看这里

## 当前状态

已经完成：

1. gem5 RISC-V O3 baseline 能跑。
2. Stream 指令 decode + functional model v1 已接入。
3. v2a ping-pong buffer / readyMap 已实现。
4. v2b readyMap 发射检查已接入当前 non-spec 路径。
5. v2c 异步访存过渡模型已实现：load refill 和 store drain 分离，memory-side 每周期调度。
6. `stream_vadd_N1024` 已跑通。
7. naive `vadd` 和 `stream_vadd` 已加入 ROI 统计。
8. `vadd` / `stream_vadd` 已改为静态输入数据，避免 ROI 前 CPU 初始化预热 L1D。
9. O3 已收紧为单 LSU，并完成 Zircon 流水线宽度近似。
10. 已扫描 MSHR 饱和点，并选定 BOOM Medium 对应的 L1D `2 MSHR`。

后续 kernel 固定使用：

```text
origin: o3_zircon_boom_medium_nopf
stream: o3_stream_axi_functional_zircon_boom_medium
```

共同关键参数：`decodeWidth=2`、单 LSU、L1D `2 MSHR`、L1I/L2
`8/32 MSHR`、`tgts_per_mshr=16`。

当前 `N=1024` VADD ROI 收敛结果：

```text
origin: 18195 cycles
stream: 11902 cycles
减少: 6293 cycles（34.59%）
加速: 1.529x
```

## 下次回来直接让 Codex 做什么

对 Codex 说：

```text
先读 notes/current_gem5_o3_baseline_spec.md 和
docs/2026-08-15_work_summary.md。VADD 配置已经收敛，请保持
decodeWidth=2、单 LSU、L1D MSHR=2，开始为下一个 kernel 准备
origin/stream 两个实现并做 ROI 对比。完成后写中文记录并提交。
```

## 下次重点

当前目标是扩展 kernel，同时保持配置和统计口径不变。

优先处理：

1. 不再修改已收敛的 CPU/cache 参数。
2. 下一个 kernel 的 origin/stream 使用同一 ROI 边界和静态输入数据。
3. 至少记录 cycles、IPC、L1D/L2 miss、MSHR 阻塞和程序自检结果。
4. 当前 stream 数据路径仍绕过 L1/L2；接入 gem5 timing memory 后必须重新
   测量，现有 `1.529x` 不是最终论文结论。

关键文档：

- `docs/o3_lsu_cycle_calibration_plan.md`
- `docs/stream_engine_v2c_async_memory_plan.md`
- `notes/current_gem5_o3_baseline_spec.md`
