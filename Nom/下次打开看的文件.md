# 下次打开先看这里

## 当前状态

已经完成：

1. gem5 RISC-V O3 baseline 能跑。
2. Stream 指令 decode + functional model v1 已接入。
3. v2a ping-pong buffer / readyMap 已实现。
4. v2b readyMap 发射检查已接入当前 non-spec 路径。
5. v2c 异步访存过渡模型已实现：load refill 和 store drain 分离，memory-side 每周期调度。
6. `stream_vadd_N1024` 已跑通。
7. naive `vadd` 和 `stream_vadd` 已加入 ROI 统计。
8. `vadd` / `stream_vadd` 已改为静态输入数据，避免 ROI 前 CPU 初始化预热 L1D。

当前 `N=1024` ROI 参考结果：

```text
naive vadd / o3_nopf:
  ROI cycles = 12175
  L1D misses = 2559
  L2 misses = 192

stream_vadd / o3_stream_axi_functional:
  ROI cycles = 11870
  loadBurstBusyCycles = 11264
```

## 下次回来直接让 Codex 做什么

对 Codex 说：

```text
先读 docs/o3_lsu_cycle_calibration_plan.md，然后从 cycle 对齐开始做。
第一步实现 single-LSU O3 配置：把 memory FU 收紧为 1 个 RdWrPort，
并把 cacheLoadPorts/cacheStorePorts 收紧为 1。做完后编译/运行
vadd_N1024 的 ROI 版，和当前 o3_nopf、stream_vadd_N1024 结果对比。
```

## 下次重点

当前目标是**尽可能校准 cycles**，不是继续把 O3 baseline 做强。

优先处理：

1. 新增 `o3_single_lsu_nopf` 配置，不覆盖当前 `o3_nopf`。
2. 跑静态数据版 `vadd_N1024`，看 naive cycles 是否上升。
3. 记录：
   - `system.cpu.numCycles`
   - `system.cpu.lsq0.loadToUse::min_value`
   - `system.cpu.lsq0.loadToUse::mean`
   - `system.cpu.commitStats0.numLoadInsts`
   - `system.cpu.commitStats0.numStoreInsts`
   - L1D / L2 miss 数
4. 如果 single-LSU 后 naive 仍偏强，再考虑收紧 L1D/L2 MSHR。

关键文档：

- `docs/o3_lsu_cycle_calibration_plan.md`
- `docs/stream_engine_v2c_async_memory_plan.md`
- `notes/current_gem5_o3_baseline_spec.md`
