## 完善 Pf-Stride degree=8 和 L2/L3 预取/缓存层次 配置
## 加入 矩阵乘 算子
## 开始着手适配

1. 架构抽象
2. 扩展指令集文档
3. 硬件文档整理

## codex写文档，stepBystep做适配

1. 先读 RTL / 软件示例 / 指令文档，写适配计划。
2. 先做功能版：decode 指令，接入 StreamEngine。
3. 跑通 `stream_vadd`，保证结果正确。
4. 建 FIFO / ready map / ping-pong buffer 访存模型。
5. v2b：在当前 non-spec 路径里加 readyMap 发射检查。
6. v2b：把 `sssAdd` 建成 3-cycle pipelined。
7. 后续：错误路径恢复 + dispatch 获取 program-order index。
8. 后续：stream 指令发射到 SE FU，不必等 ROB 队头。
9. 后续：真实 AXI / cache 接口。
10. 后续：stream op 接入完整流水线，写回 stream buffer。
